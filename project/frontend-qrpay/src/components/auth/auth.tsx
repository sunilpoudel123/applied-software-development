import React, { FC } from 'react';
import styled from 'styled-components';

const AuthWrapper = styled.div`
  /* your styles here */
`;

const Auth: FC = () => (
    <AuthWrapper>
        Auth Component
    </AuthWrapper>
);

export default Auth;